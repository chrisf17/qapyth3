message = input("Enter a message:")

encrypted_message = ""
# Write a for loop to iterate through each character in the message
# Use ord() function to get the code for char and add 1
# Convert the result back to a char using chr()
# Append the new char to encrypted_message


print(encrypted_message)
